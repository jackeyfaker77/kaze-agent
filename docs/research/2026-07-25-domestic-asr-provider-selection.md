# 桌宠国内 ASR 供应商选型

> 调研日期：2026-07-25
>
> 场景：Windows 桌面端、Electron 桌宠、按住说话，松开后将一段中文短语音转写并自动进入角色 Loop。TTS 另选供应商，不要求 ASR 和 TTS 属于同一家。

## 结论

首版推荐 **腾讯云语音识别**，优先使用“一句话识别”完成松开后的短音频转写；如果后续需要在用户说话过程中显示实时文字，再接入腾讯云实时 WebSocket 识别。

腾讯云适合首版的原因是：短音频接口的边界清楚，实时接口成熟，普通话、粤语和多种方言覆盖明确，国内网络路径和公开计费信息也更容易验证。官方公开信息显示，实时识别每月有 5 小时免费额度，一句话识别每月有 5000 次免费额度；预付费实时识别大模型 2.0 约从 1 元/小时起，一句话识别约从 3 元/千次起，具体仍以地域、版本和控制台阶梯为准。

**火山引擎/豆包语音**应作为低延迟候选。其双向流式接口适合边说边返回字符，文档建议每包发送 100–200ms 音频；流式输入模式在特定条件下给出了 300–400ms 级别的返回延迟。但其价格同时受到模型、按时长/按并发、资源包和套餐阶梯影响，不适合只凭一个公开单价做成本结论。

**阿里云智能语音交互**适合作为企业化备选。其一句话识别明确面向 1 分钟以内短语音，实时语音识别支持不限时长音频流、边说边出文字和智能断句，还支持热词与自学习语言模型。当前官方计费页已经给出资源包和后付费阶梯，价格与腾讯云接近。

没有找到 MiniMax 官方文档中明确的 ASR 产品入口。当前应保持 MiniMax 做 TTS，不要因为 TTS 已经选了 MiniMax 就假设它也承担 ASR。

## 对比

| 供应商 | 短按住说话 | 实时能力 | 中文能力 | 价格可核验性 | 判断 |
| --- | --- | --- | --- | --- | --- |
| 腾讯云 | 一句话识别适合 60 秒以内、单文件不超过 3MB 的短音频 | WebSocket 实时识别；另有实时 V2 | 普通话、粤语，以及上海话、四川话、武汉话等多种方言 | 高；免费额度和起步价公开 | 首版首选 |
| 火山引擎/豆包 | 有一句话识别和录音文件识别 | 双向流式、流式输入和优化版本；建议 100–200ms 分包 | 有普通话及大模型语音识别能力，具体模型需实测 | 中；公开计费页有完整阶梯，但模式较多 | 低延迟候选 |
| 阿里云 ISI | 一句话识别面向 1 分钟以内短语音 | 实时流式、不限时长、智能断句 | 支持热词和自学习语言模型优化 | 高；资源包和后付费阶梯已公开 | 企业化备选 |
| MiniMax | 未找到官方 ASR 入口 | 未找到可用于本需求的官方 ASR 接口 | 不作能力假设 | 不适用 | 继续只做 TTS |

## 订阅方式与价格

以下价格均为官方页面在 2026-07-25 显示的人民币价格。资源包通常有有效期，后付费需要关注是否默认开启；实际账单仍以目标地域、具体模型和控制台为准。

### 腾讯云

- **免费额度**：实时语音识别每月 5 小时，一句话识别每月 5000 次。开通当月发放，免费额度只在当月有效。
- **资源包预付费**：资源包有效期 1 年，按“免费额度 -> 预付费 -> 后付费”扣费；一句话识别为 30 千次 90 元（3 元/千次）、1000 千次 1800 元（1.8 元/千次）、10000 千次 15000 元（1.5 元/千次）、100000 千次 120000 元（1.2 元/千次）、300000 千次 300000 元（1 元/千次）。
- **一句话识别后付费**：按日用量计费，0–299 千次为 3.20 元/千次，300–999 为 2.80 元，1000–2999 为 2.20 元，3000–4999 为 1.60 元，5000 以上为 1.20 元。
- **实时识别后付费**：按日用量计费，0–299 小时为 3.20 元/小时，300–999 为 2.80 元，1000–2999 为 2.20 元，3000–4999 为 1.60 元，5000 以上为 1.20 元。实时大模型 2.0 后付费当前页面显示为 1 元/小时。
- **开通注意**：新用户后付费默认关闭，需要在语音识别控制台手动开启；关闭后付费后，预付费资源耗尽会停服而不会继续出账。

### 火山引擎/豆包语音

- **资源包预付费**：按模型购买小时资源包，通常为 1 年有效期；官方费用示例使用 1000 小时资源包 1800 元，超出部分按示例阶梯 3.5 元/小时进入后付费。这个数字是计费示例，不是所有 ASR 模型的统一单价。
- **按量后付费**：语音识别按实际音频时长累计到小时计费，具体价格随大模型流式识别、豆包流式识别 2.0、录音文件识别等模型变化。
- **并发套餐**：大模型流式语音识别并发版为 1500 元/并发/月；豆包流式语音识别模型 2.0 并发版为 500 元/并发/月。并发版只收并发费用，不再收按小时调用费用。
- **免费额度**：本次官方计费页没有确认到可直接用于短语音 ASR 的固定月度免费小时数，不能按腾讯云的 5 小时免费额度类比。

### 阿里云 ISI

- **试用**：新用户开通后可免费试用 3 个月；升级商用版后才可购买资源包或开始按量付费。
- **资源包预付费**：有效期 1 年。一句话识别为 30 千次 100 元（3.33 元/千次）、1000 千次 1800 元（1.8 元/千次）、10000 千次 15000 元（1.5 元/千次）、84000 千次 100000 元（1.2 元/千次）、182000 千次 200000 元（1.1 元/千次）、300000 千次 300000 元（1 元/千次）。实时语音识别对应相同金额阶梯，但单位为小时。
- **后付费**：一句话识别按日用量计费，0–299 千次为 3.50 元/千次，300–999 为 3.00 元，1000–2999 为 2.40 元，3000–4999 为 1.60 元，5000 以上为 1.20 元。实时语音识别对应相同阶梯，但单位为小时。
- **并发**：商用版一句话识别和实时语音识别默认 200 路并发；超出部分为 100 元/路/月或 1200 元/路/年。

### MiniMax TTS（不是 ASR）

- **按量付费**：`speech-2.8-turbo` 为 2 元/万字符，`speech-2.8-hd` 为 3.5 元/万字符。
- **语音资源包**：Turbo 为 200 万字符 360 元、2000 万字符 3400 元、2 亿字符 32000 元；HD 为 200 万字符 630 元、2000 万字符 5950 元、2 亿字符 56000 元。对应有效期为 1 个月、3 个月、1 年。
- **声音复刻**：快速复刻音色 9.9 元/音色，在首次正式用于合成时收取；复刻音色仍受认证、删除和使用期限规则约束。

## 官方事实

### 腾讯云

- 一句话识别适合 60 秒以内短音频，单文件不超过 3MB，支持本地数据上传或 URL。
- 实时 WebSocket 识别适合边说边返回文字，适合将来做录音中的实时字幕或更短的首字延迟。
- 官方计费概述列出了按时长、按次数等模式；公开免费额度包括实时识别每月 5 小时、一句话识别每月 5000 次。
- 腾讯云隐私说明要求开发者在具体功能触发时征得用户同意；音频数据加密传输，境内收集的数据存储在中国境内。

### 火山引擎/豆包语音

- 双向流式接口为 `wss://openspeech.bytedance.com/api/v3/sauc/bigmodel`，另有流式输入和优化版本。
- 文档建议每包发送 100–200ms 音频；流式输入模式说明平均 5 秒音频可在 300–400ms 内返回结果，实际桌面端延迟仍需要实测。
- 语音识别支持按音频时长计费，也提供并发版、资源包和后付费组合。官方计费示例中，1000 小时资源包价格为 1800 元，超出部分示例按 3.5 元/小时计算；这只是示例阶梯，不应直接当作所有识别模型的统一价格。
- 当前文档中列出了大模型流式识别和豆包流式语音识别模型 2.0 的资源标识，包括 `volc.bigasr.sauc.duration`、`volc.bigasr.sauc.concurrent`、`volc.seedasr.sauc.duration` 和 `volc.seedasr.sauc.concurrent`。

### 阿里云 ISI

- 一句话识别用于语音搜索、指令、短消息和智能助手等 1 分钟以内的短语音场景。
- 实时语音识别支持不限时长音频流，可以边说边返回文字，并提供智能断句。
- 热词和自学习语言模型可以用于优化角色名、专有名词、口语和项目内词汇的识别。
- 商用版支持按量计费（后付费）和资源包（预付费）；资源包有效期 1 年，额度用尽后转按量计费。
- 实时语音识别按推流时长计费，一句话识别按调用次数计费；失败调用不计费、不计入次数。
- 语言模型自学习工具当前免费开放，每位用户最多创建 10 个模型。

## 对 Shiori 的建议

首版采用下面的请求链路：

```text
按住说话
  -> 松开并结束录音
  -> 腾讯云一句话识别
  -> { text, duration, provider_request_id }
  -> 现有角色 Loop
```

首版不需要为了追求实时字幕而一开始就使用 WebSocket。桌宠的交互约定是“松开后发送”，一句话识别更简单，失败时也更容易明确提示“请重录”。之后如果首字延迟或长句体验成为问题，再把同一个 `SpeechToText` 接口替换为实时流式实现。

ASR 和 TTS 继续使用独立 provider，不做自动跨供应商 fallback。失败时记录 provider、请求耗时、音频时长和错误码，提示用户重录；不要默认保存原始音频或把完整音频写入会话。

## 实现前压测

不要只按公开价格定案。用同一批 100–200 条中文短句在三家服务上测试：

1. 安静环境、键盘声、桌面扬声器回声和远场麦克风。
2. 普通话、粤语/方言、口语省略、中英混说以及角色名和专有名词。
3. 首个可用结果、最终结果和完整请求的延迟。
4. 字错误率、漏识别率、误触发率、失败重试率和 SDK/API 集成复杂度。
5. 真实账号下的免费额度、阶梯价格、数据保留和删除政策。

## 官方来源

- 腾讯云语音识别产品文档：<https://cloud.tencent.com/document/product/1093>
- 腾讯云一句话识别：<https://cloud.tencent.com/document/product/1093/35646>
- 腾讯云实时 WebSocket 识别：<https://cloud.tencent.com/document/product/1093/48982>
- 腾讯云实时 V2 WebSocket 识别：<https://cloud.tencent.com/document/product/1093/131127>
- 腾讯云计费概述：<https://cloud.tencent.com/document/product/1093/35686>
- 腾讯云语音识别 SDK 隐私说明：<https://cloud.tencent.com/document/product/1093/73072>
- 阿里云智能语音交互产品概览：<https://help.aliyun.com/zh/isi/product-overview/what-is-intelligent-speech-interaction>
- 阿里云智能语音交互计费项：<https://help.aliyun.com/zh/isi/product-overview/pricing>
- 阿里云智能语音交互计费方式：<https://help.aliyun.com/zh/isi/product-overview/billing-10>
- 火山引擎/豆包语音文档首页：<https://www.volcengine.com/docs/6561>
- 火山引擎豆包语音计费概述：<https://www.volcengine.com/docs/6561/1359369>
- 火山引擎豆包语音计费说明：<https://www.volcengine.com/docs/6561/1359370>
- 火山引擎豆包语音流式识别文档：<https://www.volcengine.com/docs/6561/80818>
- 火山引擎豆包语音大模型流式识别文档：<https://www.volcengine.com/docs/6561/1594356>
- MiniMax API 按量计费：<https://platform.minimaxi.com/docs/guides/pricing-paygo>
- MiniMax 语音资源包：<https://platform.minimaxi.com/docs/guides/pricing-speech>

> 价格、免费额度、模型名称和接口限制会变化。本文只记录 2026-07-25 能从官方页面核实的情况；实现前必须用目标地域的真实账号重新核价和压测。
