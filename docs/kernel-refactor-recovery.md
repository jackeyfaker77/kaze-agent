# 内核重构前的恢复基线

当前可用版本保留在 `E:\CODE\hasaki-agent`，基线标签为
`pre-kernel-refactor`。本次基线只整理 Git 忽略规则和恢复说明，不改应用行为。

## 私有冷备

备份目录：`D:\Backups\hasaki-agent\20260908-083819`。
D 盘和项目所在 E 盘属于不同物理磁盘。备份包含配置和私有会话，不应上传到公共仓库。

- `project/`：修改 Git 忽略规则前的项目、工作区、依赖和构建产物。
- `python-base/`：虚拟环境依赖的 Python 3.12.13 基础解释器。
- `node/`：Node 与 pnpm 的离线工具链。
- `project-manifest.json`、`python-manifest.json`：逐文件 SHA-256 和链接清单。
- `hasaki-baseline.bundle`：首次提交及基线标签，可独立恢复 Git 历史。
- `RESTORE.md`：备份验证、恢复和回滚步骤，实际验收结果见备份中的报告。

项目中 1,612 个内部依赖链接在备份内重新定位，不指回原项目。
以下两个目录在备份前已无法读取，明确排除，其他读取或校验错误均视为失败：

- `.pytest_cache/`
- `output/pets/xuan-pet/qa/import-test-ih81kvn0/`

它们是测试缓存；源码、运行工作区、记忆和桌宠资源不受此排除影响。

## 开发隔离

上游内核验证目录：`E:\CODE\hasaki-agent-next`。
分支：`codex/kernel-refactor`。
固定上游提交：`477ef1172bd3c25085c775e8a2848d4584a8c2d2`。

新目录保留上游 Git 历史，后续 Windows 适配提交在其上叠加。
原 Hasaki 的 `workspace` 只能复制到专用迁移输入目录，不直接作为新版运行目录。
新版默认使用空白隔离工作区；历史数据库结构迁移须另行验证。
测试期间不启用真实模型调用、外部通道或主动任务。

## 回滚原则

恢复到一个不存在的新目录，验证后再切换启动路径。恢复时不得覆盖正在使用的工作区。
原代码必须配对原数据库和配置；Git 回退不负责恢复被忽略的工作区。
复制虚拟环境后，需要把副本 `.venv/pyvenv.cfg` 的 `home` 指向配对的 Python 基础解释器。
原始冷备中的文件应保持不变，只修改恢复副本。

每个迁移阶段都做独立提交；只有会话、记忆、工具、任务、桌宠和重启恢复通过验收，
才切换日常使用版本。保留旧版本及冷备，直到新版稳定运行。
