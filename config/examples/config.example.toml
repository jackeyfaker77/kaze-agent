# =============================================================================
# 必填：LLM 配置
# api_key 可以直接填字符串，也可以用 ${ENV_VAR} 引用环境变量。
# =============================================================================

[llm]

[[llm.registrations]]
id = "00000000-0000-4000-a000-000000000001"
provider = "deepseek"
model = "deepseek-v4-flash"
api_key = "sk-..."
base_url = "https://api.deepseek.com/v1"
effort = "high"

[[llm.registrations]]
id = "00000000-0000-4000-a000-000000000002"
provider = "qwen"
model = "qwen-vl-plus"
api_key = "sk-..."
base_url = "https://dashscope.aliyuncs.com/compatible-mode/v1"
effort = "none"

# =============================================================================
# 可选：桌宠语音交互
# 密钥可以直接填写，也可以使用 ${ENV_VAR}。
# =============================================================================

[voice]
enabled = false
hotkey = "Ctrl+Space"
microphone_device_id = ""

[voice.asr]
enabled = false
provider = "tencent"
base_url = "https://asr.tencentcloudapi.com/"
secret_id = ""
secret_key = ""

[voice.tts]
enabled = false
provider = "minimax"
base_url = "https://api.minimaxi.com/v1/t2a_v2"
model = "speech-2.8-turbo"
api_key = ""
volume = 2.0

# =============================================================================
# Agent 行为
# =============================================================================

[agent]
max_tokens = 8192
# 设为 0 表示不限制迭代轮数；长任务仍可用 /stop 中断。
max_iterations = 40
dev_mode = false

[desktop.chat]
# 桌面端会话回复是否实时显示 Thinking 与正文增量。
streaming_enabled = false

[agent.context]
memory_window = 40

[agent.tools]
search_enabled = true

# 可选：长期记忆整理任务（默认已启用，通常无需修改）
# [agent.maintenance]
# memory_optimizer_enabled = true
# memory_optimizer_interval_seconds = 64800

# 可选：控制加载哪些工具集（默认值适合大多数场景）
# [agent.wiring]
# toolsets = ["meta_common", "spawn", "schedule", "mcp"]

# =============================================================================
# 必填：至少配置一个频道（Telegram、NapCat QQ 或官方 QQBot 选其一）
# token / bot_uin / app_id / client_secret 为空时该频道自动跳过，不会报错。
# =============================================================================

[channels.telegram]
# BotFather 给的 bot token，格式：123456789:AAF...
token = ""
# channel 标识符，决定 session key 前缀（如 telegram:12345）。
# 多个 bot 需要区分会话时，改成不同的名字；长期记忆仍全局共享（如 "telegram_work"）。
# channel_name = "telegram"

[channels.qq]
# 填 QQ 号启用 QQ 频道，留空跳过。
bot_uin = ""
websocket_open_timeout_seconds = 5.0

# 可选：QQ 开放平台官方机器人，使用官方 Gateway 和 REST API。
# [plugins.qqbot]
# app_id = ""
# client_secret = ""
# allow_from = []

# =============================================================================
# 语义记忆
# default memory 的检索阈值在 apps/backend/plugins/default_memory/config.local.toml。
# =============================================================================

[memory]
enabled = true
# 语义记忆 engine 选择。留空等同 default。
engine = ""

[memory.embedding]
model = "text-embedding-v3"
api_key = "sk-..."
base_url = "https://dashscope.aliyuncs.com/compatible-mode/v1"

# 普通会话主动检查：在工作区 HEARTBEAT.md 中填写检查事项。
[proactive]
enabled = false
session_key = ""
default_channel = "desktop"
default_chat_id = ""
interval_seconds = 1800
